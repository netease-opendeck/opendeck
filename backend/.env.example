# OpenClaw 项目根目录（必填）
OPENCLAW_ROOT=/path/to/openclaw

# skills 相对路径，默认 workspace/skills
# OPENCLAW_SKILLS_PATH=workspace/skills

# skill-execution 文件路径（相对 OPENCLAW_ROOT），默认 memory/skill-execution.jsonl
# OPENCLAW_SKILL_EXECUTION_PATH=memory/skill-execution.jsonl

# 可选，服务端口，默认 3000
# PORT=3000
